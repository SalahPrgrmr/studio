rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      // السماح للجميع بالقراءة والكتابة مؤقتًا لتجنب أخطاء الأذونات
      allow read, write: if true;
    }
  }
}
